# Conn-unity-Group-Project

Note: Admin panel pages were implemented in `frontend/src/components/AdminPanel.jsx` (Settings, Moderation, Communities, Users). Run the frontend dev server and open `/?admin=1` to view.