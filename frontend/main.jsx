// Entrypoint moved to `src/main.jsx`.
// Keep this file as a lightweight pointer to avoid confusion.
// Use `frontend/src/main.jsx` for the React app.

/* NOTE: The canonical React entry is at:
   frontend/src/main.jsx
   The old root main.jsx has been intentionally left minimal.
*/
